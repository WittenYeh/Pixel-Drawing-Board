package com.rucjava.infoplace.ControllerModule.ControllerUtils;

public enum ClickEvent {
    /** Called when a mouse button or a finger touch is moved anywhere,
     *  but only if touchDown previously returned true for the mouse button or touch.
     *  The touchDragged event is always handled. */
    TOUCH_DRAGGED,
}
